Second upload test.

Basicly you gotta check whats left to upload by:
-git status
  check if something is "modified"
-git add FILENAME
-git commit -m "comment about the upload"
-git push
  to upload content with credentials.
